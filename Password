import random
import string
a=int(input("Enter the length:"))
char=string.ascii_letters
char+=string.digits
char+=string.punctuation
password=""
for i in range(a):
    password+=random.choice(char)
print("Random Genereated Password: ",password)
